# Go build and test artifacts
/lorekeep
/lorekeep.exe
*.exe
*.test
*.out
# Derived build output, at any level: a world repo builds into its own build/.
build/

# Per-developer agent and editor tooling
/.serena/
/.idea/
/.vscode/
