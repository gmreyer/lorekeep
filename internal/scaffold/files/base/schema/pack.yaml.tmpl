# The project pack: this world's vocabulary on top of the core pack that
# lorekeep embeds.
#
# core_version records which core vocabulary the lore was written against.
# It is a separate fact from the lorekeep version in lorekeep-version, and
# lorekeep build reports when the two drift.
name: {{.Name}}
version: 0.1.0
core_version: {{.CoreVersion}}
description: Project pack. Empty until the vocabulary is authored.
